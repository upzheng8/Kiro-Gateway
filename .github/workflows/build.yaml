name: Build Tauri App

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-latest
            target: aarch64-apple-darwin
            args: "--target aarch64-apple-darwin"
            bundles: "dmg"
          - platform: macos-latest
            target: x86_64-apple-darwin
            args: "--target x86_64-apple-darwin"
            bundles: "dmg"
          - platform: windows-latest
            target: x86_64-pc-windows-msvc
            args: ""
            bundles: "nsis"
          - platform: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
            args: ""
            bundles: "deb"

    runs-on: ${{ matrix.platform }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Tauri v2 Linux 依赖
      - name: Install Linux dependencies
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: "./src-tauri -> target"
          prefix-key: "v8-${{ matrix.platform }}-${{ matrix.target }}"

      - name: Install dependencies
        run: npm ci

      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          projectPath: ./
          tauriScript: npm run tauri
          args: --verbose ${{ matrix.args }} --bundles ${{ matrix.bundles }}

      - name: Upload Release (Windows)
        if: matrix.platform == 'windows-latest' && startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          draft: false
          prerelease: false
          body: |
            ## 🚀 Kiro Gateway ${{ github.ref_name }} 发布
            1. Kiro Gateway具有客户端相关操作功能 和反代功能
            2. 可进行模型锁定，机器码重置，定时刷新token
            3. 可客户端上号，刷新，余额显示，状态显示，有效期显示
            4. 可批量导入json，或一行一个Refresh Token
            5. 可分组进行管理账号
            6. 反代功能来自kiro.rs进行优化，可指定分组反代，随用随启
            7. bug等优化问题请查看关于加群反馈或github issues

            ### 下载说明
            | 平台 | 文件名 | 说明 |
            |---|---|---|
            | **Windows** | `.exe` | 安装版 |
            | **macOS** | `_aarch64.dmg` | Apple Silicon (M1/M2/M3) |
            | **macOS** | `_x64.dmg` | Intel Mac |
            | **Linux** | `.deb` | Debian/Ubuntu |

          files: src-tauri/target/${{ matrix.target }}/release/bundle/nsis/*.exe

      - name: Upload Release (Others)
        if: matrix.platform != 'windows-latest' && startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          files: |
            src-tauri/target/${{ matrix.target }}/release/bundle/dmg/*.dmg
            src-tauri/target/${{ matrix.target }}/release/bundle/deb/*.deb
