# 项目上下文信息

- ## 分组功能和代理启停功能开发进度 (2026-01-12)

### 已完成
1. **分组管理**：添加/删除/重命名分组、选择分组、批量转移分组
2. **添加凭证时选择分组**
3. **刷新按钮只刷新当前分组凭证**
4. **反代配置页面**：分组选择、启停开关 UI

### 代理启停功能（进行中）
- 后端：`AdminState` 添加 `ProxyController` 和 `proxy_enabled` 标志
- 后端：`anthropic/middleware.rs` 检查 `proxy_enabled` 标志
- 后端：`run_admin_server` 已合并反代功能
- 前端：开关 UI 和 API 已完成

### 待测试
- 代理启停功能需要完整测试
- 分组筛选和凭证轮询逻辑
